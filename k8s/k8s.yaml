---
apiVersion: v1
kind: Service
metadata:
  name: obs-bingo
  namespace: obs-bingo
  labels:
    app: obs-bingo
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: obs-bingo
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: obs-bingo
  namespace: obs-bingo
  labels:
    app: obs-bingo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: obs-bingo
  template:
    metadata:
      labels:
        app: obs-bingo
    spec:
      containers:
      - name: obs-bingo
        image: ghcr.io/dilaz/obs-bingo:6ad523f8a360782681a0348b0e675c90d67babba
        imagePullPolicy: IfNotPresent
      imagePullSecrets:
      - name: github-container-registry
